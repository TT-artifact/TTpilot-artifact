{
  "name": "tt-overhead-eval",
  "version": "1.0.0",
  "private": true,
  "description": "Load time overhead measurement for TT-patched apps",
  "scripts": {
    "measure": "ts-node scripts/measure.ts",
    "raw-invocation": "ts-node scripts/raw-invocation/index.ts",
    "test:fixtures": "ts-node scripts/raw-invocation/fixtures/run-fixtures.ts",
    "postinstall": "playwright install chromium"
  },
  "devDependencies": {
    "@playwright/test": "1.61.0",
    "@types/node": "^20.10.0",
    "ts-node": "^10.9.0",
    "typescript": "^5.3.0"
  }
}
